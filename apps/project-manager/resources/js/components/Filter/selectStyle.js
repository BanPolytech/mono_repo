const selectStyle = theme => ({
    formControl: {
        margin: theme.spacing(1),
        minWidth: 80
    },
    selectEmpty: {
        marginTop: theme.spacing(2),
    },
})

export default selectStyle;
